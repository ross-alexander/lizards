/**
 ** The Build/Destroy order functions.
 **/

int build_fertile_order (world_t*, order_t *order);
int build_den_order (world_t*, order_t *order);
int build_bridge_order (world_t*, order_t *order);
int destroy_fertile_order (world_t*, order_t *order);
int destroy_bridge_order (world_t*, order_t *order);
int build_raft_order (world_t*, order_t *order);
